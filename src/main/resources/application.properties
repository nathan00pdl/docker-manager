spring.application.name=docker-manager

docker.socket.path=unix:///var/run/docker.sock

# Full control of the Docker daemon, with no authentication: listen on this machine only.
# Set SERVER_ADDRESS=0.0.0.0 to expose it, and only on a network you trust.
server.address=${SERVER_ADDRESS:127.0.0.1}
